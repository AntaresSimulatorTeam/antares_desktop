name: release
on:
  release:
    types: [published]
  push:
    branches:
      - "**"

jobs:
  binary:
    runs-on: ${{ matrix.os }}
    strategy:
      max-parallel: 3
      matrix:
        os: [windows-latest]

    steps:
      - name: 🐙 Checkout GitHub repo (Also build submodule AntaREST)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: 🔗 Install wget for Windows
        if: matrix.os == 'windows-latest'
        run: choco install wget --no-progress

      - name: 💚 Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0

      - name: 💚 Install dependencies
        run: npm install
        working-directory: antares_web/webapp

      - name: 💚 Build webapp
        run: bash ./build-front.sh
        working-directory: scripts
        env:
          NODE_OPTIONS: --max-old-space-size=8192

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: 🐍 Install development dependencies
        run: |
          python -m pip install uv
          cd antares_web/
          uv sync --all-extras
          cd ..

      - name: 📦 Packaging
        run: bash ./package_antares_web.sh
        working-directory: scripts

      - name: 📜️ Copy changelog file
        run: |
          cp antares_web/docs/CHANGELOG.md dist/package/CHANGELOG.md

      - name: 📦 Archive Antares Desktop for Windows
        if: matrix.os == 'windows-latest'
        run: 7z a AntaresWeb-${{ matrix.os }}-${{ github.ref_name }}.zip *
        working-directory: dist/package

      - name: 📦 Archive Antares Desktop for Ubuntu
        # this is the only way to preserve file permission and symlinks
        if: matrix.os != 'windows-latest'
        run: zip -r --symlinks AntaresWeb-${{ matrix.os }}-${{ github.ref_name }}.zip *
        working-directory: dist/package

      - name: 📦 Upload asset to release
        run: gh release upload v2.30.0 dist/package/AntaresWeb-${{ matrix.os }}-v2.30.0.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}